package me.deamonet.mi.entity;

public class BilibiliDynamic extends Article {
    public BilibiliDynamic(String link, String title, String author){
        super();
        this.setTitle(title);
        this.setAuthor(author);
        this.setLink(link);
    }
}
